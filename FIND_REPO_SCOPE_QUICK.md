# 🎯 Quick Guide: Find "repo" Scope in GitHub

## 📍 Direct Link:
**https://github.com/settings/tokens**

---

## 🚀 Quick Steps:

### 1️⃣ Go to Token Page
Click: https://github.com/settings/tokens

### 2️⃣ Click "Generate new token"
Then click: **"Generate new token (classic)"**

### 3️⃣ Find "repo" Scope
Scroll down to **"Select scopes"** section

Look for:
```
✅ repo
   Full control of private repositories
```

### 4️⃣ CHECK the box ✅
Make sure **`repo`** checkbox is checked!

---

## 📋 Navigation Path:

```
GitHub.com → Profile → Settings → Developer settings → Personal access tokens → Tokens (classic)
```

Then click **"Generate new token"** → Scroll to **"Select scopes"** → Check **`repo`** ✅

---

## ✅ What You Should See:

When creating a token, you'll see a section like this:

```
Select scopes

☑️ repo  ← CHECK THIS ONE!
   Full control of private repositories

☐ workflow
☐ write:packages
... (more options)
```

**Make sure the box next to `repo` is CHECKED!** ✅

---

## 🔍 To Check Existing Token:

1. Go to: https://github.com/settings/tokens
2. Click **"Tokens (classic)"**
3. Find your token (e.g., "Nirvista Project")
4. Click on the token name
5. Look at **"Scopes"** section - should show `repo`

**If it doesn't show `repo`, create a new token with `repo` checked!**

---

**That's it! The `repo` scope is in the "Select scopes" section when creating/editing tokens.** ✅